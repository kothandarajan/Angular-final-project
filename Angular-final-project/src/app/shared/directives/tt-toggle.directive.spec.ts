import { ttToggleDirective } from './tt-toggle.directive';

describe('TtToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ttToggleDirective();
    expect(directive).toBeTruthy();
  });
});
